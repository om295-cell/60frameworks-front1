# API Base URL (empty uses current domain relative /api/v1 with Vite proxy or production domain)
VITE_API_BASE_URL=/api/v1

# Optional production backend endpoint (e.g. deployed on Railway, Render, Fly.io, AWS)
# VITE_API_BASE_URL=https://api.your-creative-agency.com/api/v1
